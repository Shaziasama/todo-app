apiVersion: v1
kind: Service
metadata:
  name: nextjs-app-service
  labels:
    app: nextjs-app
spec:
  type: {{ .Values.nextjsApp.service.type }}
  ports:
  - port: {{ .Values.nextjsApp.service.port }}
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: nextjs-app